{
  "name": "express-web-template",
  "version": "0.0.1",
  "private": true,
  "scripts": {
      "watch": "watchify -g ejs-browserify-transformer -g require-globify load.js -o loaded.js -v"
  },
  "dependencies": {
    "express-web": "git://github.com/starterstep/express-web.git"
  },
  "devDependencies": {
    "require-globify": "1.3.0"
  }
}